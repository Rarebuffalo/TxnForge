# Database URL configuration (replace with your Postgres credentials/hosts)
DATABASE_URL="postgresql://postgres:password@localhost:5432/txnforge"

# Server configuration
PORT=8000
FRONTEND_URL="http://localhost:3000"

# Better Auth configuration (replace secret with a secure key)
BETTER_AUTH_SECRET="your_better_auth_secret_key"
BETTER_AUTH_URL="http://localhost:8000"
